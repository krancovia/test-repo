name: Slow CI

on:
  merge_group:

jobs:
  slow-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Simulate slow build
        run: |
          echo "Starting slow build..."
          sleep 60
          echo "Build complete!"
      
      - name: Simulate slow tests
        run: |
          echo "Running slow tests..."
          sleep 60
          echo "Tests complete!"
